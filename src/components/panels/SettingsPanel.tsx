import { Panel } from '../Panel';

export function SettingsPanel() {
    return (
        <Panel title="Settings">
            <p className="panel-placeholder">Settings options go here.</p>
        </Panel>
    );
}
